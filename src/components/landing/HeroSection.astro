---
// HeroSection.astro - Główna sekcja hero z komunikatem produktu i CTA
// Komponent statyczny (Astro), bez React islands
// Updated with Fluent 2.0 Professional Design System
---

<header class="relative py-20 md:py-32 overflow-hidden">
  <!-- Gradient Background with subtle pattern -->
  <div class="absolute inset-0 bg-gradient-to-br from-background via-surface-base to-accent/5"></div>

  <!-- Decorative elements for depth -->
  <div class="absolute top-0 right-0 w-96 h-96 bg-primary/5 rounded-full blur-3xl"></div>
  <div class="absolute bottom-0 left-0 w-96 h-96 bg-accent/10 rounded-full blur-3xl"></div>

  <div class="container mx-auto px-4 relative z-10">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Main Heading with Fluent Typography -->
      <h1 class="text-display text-foreground mb-6 tracking-tight">
        Inteligentny asystent prawny oparty na AI
      </h1>

      <!-- Subtitle -->
      <p class="text-body-large text-muted-foreground mb-4 max-w-2xl mx-auto">
        Zadawaj pytania w języku naturalnym i otrzymuj precyzyjne odpowiedzi oparte na polskich aktach prawnych.
      </p>

      <!-- Supporting text -->
      <p class="text-body text-muted-foreground/80 mb-12 max-w-xl mx-auto">
        Wspieramy Cię w analizie 20 000 najnowszych ustaw i rozporządzeń.
      </p>

      <!-- CTA Buttons with elevation -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a
          href="/register"
          class="inline-flex items-center justify-center gap-2 whitespace-nowrap font-semibold ring-offset-background transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 bg-primary text-primary-foreground hover:bg-primary-hover active:bg-primary-active rounded-lg shadow-md hover:shadow-lg active:shadow-sm duration-normal ease-standard h-[var(--button-height-lg)] px-[var(--button-padding-x-lg)] text-body-large"
          aria-label="Wypróbuj PrawnikGPT za darmo - przejdź do rejestracji"
        >
          Wypróbuj za darmo
        </a>
        <a
          href="/login"
          class="inline-flex items-center justify-center gap-2 whitespace-nowrap font-semibold ring-offset-background transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2 border-2 border-primary bg-background text-primary hover:bg-primary hover:text-primary-foreground active:bg-primary-active rounded-lg shadow-sm hover:shadow-md duration-normal ease-standard h-[var(--button-height-lg)] px-[var(--button-padding-x-lg)] text-body-large"
          aria-label="Zaloguj się do istniejącego konta"
        >
          Zaloguj się
        </a>
      </div>
    </div>
  </div>
</header>

