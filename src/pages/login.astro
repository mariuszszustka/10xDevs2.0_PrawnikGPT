---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Logowanie - PrawnikGPT">
  <main class="min-h-screen flex items-center justify-center bg-gray-50">
    <div class="w-full max-w-md p-8 bg-white rounded-lg shadow-md">
      <h1 class="text-3xl font-bold text-gray-900 mb-6 text-center">
        Logowanie
      </h1>
      <p class="text-gray-600 mb-8 text-center">
        Zaloguj się, aby uzyskać dostęp do PrawnikGPT
      </p>
      
      <!-- TODO: Dodać React island dla formularza logowania -->
      <form class="space-y-4">
        <div>
          <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
            Email
          </label>
          <input
            type="email"
            id="email"
            name="email"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
            placeholder="twoj@email.pl"
          />
        </div>
        
        <div>
          <label for="password" class="block text-sm font-medium text-gray-700 mb-2">
            Hasło
          </label>
          <input
            type="password"
            id="password"
            name="password"
            required
            class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
            placeholder="••••••••"
          />
        </div>
        
        <button
          type="submit"
          class="w-full px-4 py-2 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition-colors"
        >
          Zaloguj się
        </button>
      </form>
      
      <p class="mt-6 text-center text-sm text-gray-600">
        Nie masz konta?{' '}
        <a href="/register" class="text-blue-600 hover:text-blue-700 font-medium">
          Zarejestruj się
        </a>
      </p>
    </div>
  </main>
</BaseLayout>

